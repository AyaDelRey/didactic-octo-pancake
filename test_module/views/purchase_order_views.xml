<odoo>
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.export.edi</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <header>
                <button name="export_to_edi"
                        type="object"
                        string="Exporter en EDI"
                        class="oe_highlight"/>
            </header>
        </field>
    </record>
</odoo>
<!-- This code adds a button to the purchase order form view that allows users to export the order to EDI format. -->
